import 'package:flutter/material.dart';

class ItemInfoGroupLink extends StatelessWidget {
  const ItemInfoGroupLink({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
